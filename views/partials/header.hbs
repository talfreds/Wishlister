<header class="header shadow">
  <div id="logo">
    <img id="steam" src='game.png'>
  </div>
  <div id="title">Wishlister</div>
  {{#if loggedIn}}
    {{> loggedin_message}}
  {{else}}
  	{{#if creatingUser}}
  	{{else}}
  		{{#if noLogIn}}
  		{{else}}
    	{{> login_form}}
  		{{/if}}
    {{/if}}
  {{/if}}
</header>
